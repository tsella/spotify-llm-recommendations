<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container">
    <a class="navbar-brand" href="/">
      <i class="bi bi-spotify me-2"></i>
      Spotify LLM Recommendations
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link <%= active === 'dashboard' ? 'active' : '' %>" href="/spotify/dashboard">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= active === 'recommendations' ? 'active' : '' %>" href="/recommendations">Recommendations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= active === 'create-playlist' ? 'active' : '' %>" href="/playlists/create">Create Playlist</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= active === 'my-playlists' ? 'active' : '' %>" href="/playlists/list">My Playlists</a>
        </li>
      </ul>
      <% if (user) { %>
        <div class="d-flex align-items-center">
          <% if (user.imageUrl) { %>
            <img src="<%= user.imageUrl %>" alt="Profile" class="profile-img me-2">
          <% } else { %>
            <i class="bi bi-person-circle me-2 fs-4"></i>
          <% } %>
          <span class="me-3"><%= user.name %></span>
          <a href="/auth/logout" class="btn btn-outline-light btn-sm">
            <i class="bi bi-box-arrow-right me-1"></i> Logout
          </a>
        </div>
      <% } else { %>
        <a href="/auth/login" class="btn btn-spotify btn-sm">
          <i class="bi bi-spotify me-1"></i> Login
        </a>
      <% } %>
    </div>
  </div>
</nav>